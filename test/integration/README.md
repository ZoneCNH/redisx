# L2 Integration

本目录预留 `redisx` 的 L2 integration profile。

当前仓库没有默认 Redis 网络连接实现，也不声明真实 provider integration 已通过。后续如果接入 Redis 容器或外部服务，测试必须把连接配置限制在本地测试环境，并将报告写入 `.agent/evidence/l2/integration-report.json`。
