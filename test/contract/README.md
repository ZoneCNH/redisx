# L2 Contract

本目录保存 `redisx` 的 L2 contract profile 本地检查。

当前测试验证 `.agent/l2-capabilities.yaml`、`.agent/evidence/l2/release-readiness.json` 与 `.agent/evidence/l2/contract-report.json` 的形状和真实状态，要求 `score=100`、`release_ready=true`，且 `contract` profile 为 `pass`。
